apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: gateway-kong-http
  # Next line active IP restriction plugin named rule
  #konghq.com/plugins: ipr-test-network-only
spec:
  gatewayClassName: gwclass-kong
  listeners:
  - name: kong-http 
    port: 80
    protocol: HTTP
